{
  "name": "koreacars-mobile-analyzer",
  "version": "1.0.0",
  "description": "Koreacars.de-Fahrzeuge laden, vergleichbare Autos auf mobile.de finden und Wiederverkaufs-Margen berechnen – als lokale Web-App und Telegram-Bot, optional mit lokalem LLM (Ollama).",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "web": "node server.mjs",
    "bot": "node bot.mjs",
    "check": "node --check server.mjs && node --check bot.mjs && node --check lib/extract.mjs && node --check lib/search.mjs && node --check lib/analyze.mjs && node --check lib/templates.mjs && node --check lib/llm.mjs",
    "selftest": "node bot.mjs --selftest"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "puppeteer-core": "^24.43.1"
  }
}
