# Telegram-Bot-Token (Pflicht für den Bot): hole es bei @BotFather
TELEGRAM_BOT_TOKEN=

# Optional: Web-Port
PORT=8787

# Optional: Lokales LLM (Ollama) steuern – "1" schaltet es aus
LLM_DISABLE=0

# Optional: Modellname, wenn du ein anderes Ollama-Modell nutzt
LLM_MODEL=qwen2.5:7b
